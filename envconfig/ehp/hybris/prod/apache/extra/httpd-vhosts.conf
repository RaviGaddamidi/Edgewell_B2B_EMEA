#
# Virtual Hosts
#
# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.2/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# Use name-based virtual hosting.
#

KeepAlive on
KeepAliveTimeout 6000
NameVirtualHost energizer.com:80
NameVirtualHost energizer.com:443

AddType application/x-x509-ca-cert .crt
AddType application/x-pkcs7-crl    .crl
#SSLSessionCacheTimeout  300
#SSLMutex default
#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#
<VirtualHost energizer.com:80>
        ServerAdmin customerservice@enr.com
        DocumentRoot "W:\valid_folder_path"
        ServerName energizer.com
       
		
		RewriteEngine on
		RewriteRule ^/?(.*) https://%{HTTP_HOST}:443/$1 [L,R,NE]
				
        ProxyPass / http://live.energizer.com:9001/
        ProxyPassReverse / http://live.energizer.com:9001/

        ErrorLog logs/live.energizer.com-error.log
        CustomLog logs/live.energizer.com-access.log common
</VirtualHost>
<VirtualHost live.energizer.com:443>
        ServerAdmin customerservice@enr.com
        DocumentRoot "W:\valid_folder_path"
        ServerName live.energizer.com
        
		
		SSLEngine on
        SSLProxyEngine On

        SSLProtocol all -SSLv2
        SSLCipherSuite HIGH:MEDIUM:!aNULL:!MD5
        SSLCertificateFile "W:/certificate_path/liveenergizer.crt"
        SSLCertificateKeyFile "W:/certificate_key_path/liveenergizer.key"

        RewriteEngine on  
		RewriteRule ^/?(reportcockpit|admincockpit|cscockpit|cmscockpit|productcockpit|mcc) -  [R=403,L,F]
		RewriteRule ^/?(oci|solrfacetsearch|hmc|hmc_junit|text|acceleratorservices|rest) -  [R=403,L,F]
		RewriteRule ^/?(test|maintenance|hac|hac_junit|virtualjdbc|backoffice|bmecat) -  [R=403,L,F]
		
        ProxyPass / https://live.energizer.com:9002/
        ProxyPassReverse / https://live.energizer.com:9002/

        ErrorLog logs/ssl-live.energizer.com-error.log
        CustomLog logs/ssl-live.energizer.com-access.log common
</VirtualHost>